# martial-arts-class-data-management
A full-stack martial arts class management system — MySQL database with 7 relational tables (students, coaches, classes, attendance, tournaments) and a Flask + HTML frontend for CRUD operations, built as a DBMS project.
